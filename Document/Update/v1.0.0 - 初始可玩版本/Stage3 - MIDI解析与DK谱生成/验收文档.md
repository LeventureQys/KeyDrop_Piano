# Stage 3 - MIDI 解析与 DK 谱生成 · 验收文档

> 按《Agent 开发规范》6.3"零上下文可验收"标准编写。新验收 Agent 仅凭本文档 + 仓库状态 + 已装 Flutter SDK 即可验收。**无硬件依赖、无用户人工门禁。**

## 0. 元信息
- **Stage**：3
- **关联**：[Stage 3 设计文档](设计文档.md) / [Version 设计文档](../设计文档.md)
- **验收形式**：命令行自动化（analyze/test/layering）+ 文件存在性。

## 1. 验收范围
### 1.1 覆盖
| 项 | 形式 |
|---|---|
| SMF 格式 0/1 解析正确性 | 单元测试 M1–M4、M7、M8 |
| 不支持/损坏触发异常 | 单元测试 M5、M6 + 空 note 用例 |
| DK 谱生成（去重/选轨/范围过滤/meta） | 单元测试 G1–G6 |
| 领域层零平台依赖 | `dart run tool/check_layering.dart` |
| fixture 完整性 | 文件检查（≥5 个 .mid） |
| 静态分析 0 警告 | `flutter analyze` |

### 1.2 不覆盖
| 项 | 原因 |
|---|---|
| 文件路径读取 / SAF | 属平台层 Stage 4 |
| UI 转换页 | Stage 5 |
| 真实第三方复杂 MIDI 兼容 | v1.0.0 以合成 fixture 覆盖核心路径，复杂兼容留后续 |

## 2. 验收环境
| 项 | 规格 |
|---|---|
| Flutter | 3.44.3（PATH 含 `C:\flutter\bin`） |
| Android SDK | 不需要 |
| 工作目录 | `app/`（flutter 命令）；仓库根（文件检查） |

## 3. 前置条件
- [ ] Stage 2 已通过（领域模型/异常就绪）。
- [ ] `dart run tool/gen_midi_fixtures.dart` 已执行，`test/fixtures/` 已生成。

## 4. 验收项
> flutter 命令 workdir=`app`。证据存 `Stage3 - MIDI解析与DK谱生成/验收证据/`。

### S3.1 关键文件存在性
- **步骤**（仓库根）：检查以下文件存在且非空：
  - `app/lib/domain/services/midi_parser.dart`
  - `app/lib/domain/services/dk_generator.dart`
  - `app/lib/domain/models/midi_data.dart`
  - `app/tool/gen_midi_fixtures.dart`
  - `app/test/domain/services/midi_parser_test.dart`
  - `app/test/domain/services/dk_generator_test.dart`
  - `app/test/fixtures/` 下 `.mid` 文件数量 ≥ 5
  - `Document/.../Stage3 .../不支持文件处理.md`
- **通过标准**：全部存在；.mid ≥ 5。
- **证据**：`验收证据/S3.1.log`。

### S3.2 fixture 生成可重复
- **步骤**（workdir=app）：`dart run tool/gen_midi_fixtures.dart`
- **期望**：exit 0，打印生成的文件清单（≥5）。
- **通过标准**：exit 0 且 `test/fixtures/` ≥ 5 个 .mid。
- **证据**：`验收证据/S3.2.log`。

### S3.3 静态分析 0 警告
- **步骤**（workdir=app）：`flutter analyze`
- **期望**：`No issues found!`。
- **证据**：`验收证据/S3.3.log`。

### S3.4 单元测试全绿
- **步骤**（workdir=app）：`flutter test`
- **期望**：`All tests passed!`，含 M1–M8（解析）、G1–G6（生成）、空 note 异常用例，以及 Stage 2 既有测试全部仍通过（回归）。
- **通过标准**：exit 0。
- **证据**：`验收证据/S3.4.log`。

### S3.5 分层约束
- **步骤**（workdir=app）：`dart run tool/check_layering.dart`
- **期望**：`LAYERING OK`（services / models 无平台依赖）。
- **证据**：`验收证据/S3.5.log`。

### S3.6 异常文案清单
- **步骤**：打开 `不支持文件处理.md`，核对 5 类触发条件均有「异常类 + UI 文案」。
- **通过标准**：5 类齐全且与 `midi_parser.dart` 实际抛出的异常一致。
- **证据**：`验收证据/S3.6.log`（grep 异常类名计数）。

## 5. 失败与阻塞处理
| 场景 | 处理 |
|---|---|
| S3.3 有 warning | 回退修复，禁止豁免 |
| S3.4 测试失败 | 定位 SubStage 修复 |
| S3.5 分层违规 | 移除违规 import |
| fixture 生成失败 | 修 `gen_midi_fixtures.dart`，最多 3 次 |

## 6. 回归范围
- Stage 2 全部单元测试必须仍通过（`flutter test` 已含）。

## 7. 交付物检查
| 文件 | 必存 | 来源 |
|---|---|---|
| `midi_parser.dart`/`dk_generator.dart`/`midi_data.dart` | ✓ | 3.1/3.2 |
| `gen_midi_fixtures.dart` + `test/fixtures/*.mid`(≥5) | ✓ | 3.1 |
| `midi_parser_test.dart`/`dk_generator_test.dart` | ✓ | 3.1/3.2 |
| `不支持文件处理.md` | ✓ | 3.3 |
| `验收证据/S3.1~S3.6` | ✓ | 验收 Agent |
| `完成报告.md` | ✓ | MainAgent |

## 8. 结论模板
```markdown
## Stage 3 验收结论
| 验收项 | 结果 |
|---|---|
| S3.1 文件存在 | 通过/失败 |
| S3.2 fixture 生成 | 通过/失败 |
| S3.3 静态分析 | 通过/失败 |
| S3.4 单元测试 | 通过/失败 |
| S3.5 分层约束 | 通过/失败 |
| S3.6 异常文案 | 通过/失败 |
- 总结论：通过/不通过/阻塞
- 验收时间：YYYY-MM-DD
```
