# Stage 5 - UI 谱面库 / 设置 / 转换页 · 验收文档

> 按《Agent 开发规范》6.3"零上下文可验收"标准编写。验收对象为 Stage 5 设计文档
> 界定的三个非播放页面与 Provider 层。

## 0. 验收范围

- 覆盖：`lib/ui/pages/library_page.dart`、`settings_page.dart`、`convert_page.dart`、
  `lib/di/providers.dart`、`lib/platform/debug/in_memory_fs_port.dart`。
- 不覆盖：下落播放器（Stage 6）、Kotlin 平台插件（Stage 4）。

## 1. 验收环境

- Flutter 3.x stable；`flutter pub get` 成功。
- 测试运行于宿主平台（Windows 桌面测试环境），文件系统与 MIDI 输入用
  `InMemoryFileSystemPort` / `DebugMidiInjector` 覆盖 Provider 注入。

## 2. 验收项

### S5-1. 库页空态与列表（对应 T1/T3）
- **操作**：`flutter test test/ui/library_page_test.dart`。
- **期望**：空库显示"还没有任何谱面"引导；有谱时列表展示并可前缀/子串搜索。
- **证据**：测试全绿输出。

### S5-2. 转换五状态机闭环（对应 T3）
- **操作**：`flutter test test/ui/convert_flow_test.dart`。
- **期望**：注入 MIDI 字节 → 选轨（含"合并全部轨道"）→ 预览（BPM/拍号/调号/时长/
  sha256）→ 保存 → 谱面库出现新条目；格式 2 / 损坏文件 → 错误态并显示对应文案。
- **证据**：测试全绿输出。

### S5-3. 设置持久化
- **操作**：`flutter test test/platform/android/android_config_repository_test.dart`
  + 设置页 Widget 操作（控件触发 `AppConfigNotifier._save` → shared_preferences）。
- **期望**：`load → save → load` round-trip 一致；未知枚举值回退默认不抛异常。
- **证据**：测试全绿输出。

### S5-4. 静态质量
- **操作**：`flutter analyze`。
- **期望**：`No issues found!`。

## 3. 结论（v1.0.1 补完轮次）

| 项 | 结果 |
|---|---|
| S5-1 | ✅ 通过（`test/ui/library_page_test.dart` 3 用例） |
| S5-2 | ✅ 通过（`test/ui/convert_flow_test.dart` 3 用例，含错误路径） |
| S5-3 | ✅ 通过（`test/platform/android/android_config_repository_test.dart` 3 用例） |
| S5-4 | ✅ 通过（`flutter analyze` 0 问题） |
