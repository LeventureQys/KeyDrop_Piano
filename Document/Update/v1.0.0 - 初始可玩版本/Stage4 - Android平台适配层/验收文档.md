# Stage 4 - Android 平台适配层 · 验收文档

> 按《Agent 开发规范》6.3"零上下文可验收"标准。**本 Stage 有硬件依赖（USB MIDI 需真机 + 电钢琴），硬件手动验收留到 Stage 7**；自动化验收仅验证编译通过 + 适配器单元测试 + 分层。

## 0. 元信息
- **Stage**：4
- **关联**：[Stage 4 设计文档](设计文档.md) / [Version 设计文档](../设计文档.md)

## 1. 验收范围
| 覆盖 | 形式 | 不覆盖 | 原因 |
|---|---|---|---|
| APK 编译成功 | `flutter build apk --debug` exit 0 | USB MIDI 真机连接 | Stage 7 |
| 适配器单元测试（mock channel） | `flutter test` | SAF 真机文件选择 | Stage 7 |
| 分层自检 | `dart run tool/check_layering.dart` | 真机延迟/兼容性 | Stage 7 |

## 2. 验收环境
Flutter 3.44.3、Android SDK 36.1.0、JDK 21（AS JBR）、workdir=`app/`

## 3. 验收项

### S4.1 关键文件存在性
检查文件：`MidiInputPlugin.kt`、`FileSystemPlugin.kt`、`android_midi_input_adapter.dart`、`android_file_system_adapter.dart`、`android_config_repository.dart`、`debug_midi_injector.dart`、`MainActivity.kt`（已修改）、`AndroidManifest.xml`（含 usb/midi feature）。8 项全存在且非空。

### S4.2 APK 编译
`flutter build apk --debug` → exit 0（首次需下载 Gradle，延长超时）。

### S4.3 单元测试
`flutter test` → `All tests passed!`（含 Stage 2/3/4 回归）。

### S4.4 分层
`dart run tool/check_layering.dart` → `LAYERING OK`。

## 4. 证据目录
证据保存到 `Stage4 - Android平台适配层/验收证据/`。

## 5. 结论模板
| S4.1 文件存在 | S4.2 编译 | S4.3 测试 | S4.4 分层 | 总结论 |
|---|---|---|---|---|
| 通过/失败 | 通过/失败 | 通过/失败 | 通过/失败 | 通过/不通过 |
