# Stage 7 - 集成测试与收尾 · 验收文档

> 按《Agent 开发规范》6.3"零上下文可验收"标准编写。验收对象为 Stage 7 设计文档
> 界定的集成测试、真机验收指南与 Version 收尾产物。

## 0. 验收范围

- 覆盖：`app/integration_test/player_test.dart`（V3 场景 A-E）、
  `真机验收指南.md`、Version 总结报告、`test_fixtures/`、`scripts/check_layering.sh`。
- 不覆盖：真机执行（V5-V10，需用户配合，见真机验收指南）。

## 1. 验收环境

- 自动化部分：Flutter 3.x stable + `flutter test integration_test/`，
  需已连接 Android 设备或模拟器（debug 构建含 DebugMidiInjector）。
- 代码静态部分：`flutter analyze` / `flutter test` / `dart run tool/check_layering.dart`。

## 2. 验收项

### S7-1. V3 场景 A：MIDI → DK 谱 golden 对比
- **操作**：`flutter test integration_test/` 中 `V3-A` 用例（宿主环境可由
  `flutter test test/domain/services/acceptance_fixtures_test.dart` 等效验证）。
- **期望**：`simple_format0_8bars.mid` 生成谱与 `golden_simple_format0_8bars.dk.json`
  byte-equal（忽略空白）。

### S7-2. V3 场景 B/C/E：伪 MIDI 注入判定
- **操作**：`flutter test integration_test/` 中 `V3-B`（演奏模式全 perfect、
  掉 key=0、错音=0）、`V3-C`（学习模式错 1 次继续到下一 note）、
  `V3-E`（2.0x 时间线 = 原 t × 0.5）。
- **期望**：三者全部 PASS。

### S7-3. V3 场景 D：不支持格式弹窗
- **操作**：`V3-D` 用例。
- **期望**：`UnsupportedMidiFormatException` 抛出且转换页错误态显示"格式 2"文案。

### S7-4. 交付物完整性（Version V11）
- **操作**：检查 `scripts/check_layering.sh`、`test_fixtures/`（7 文件）、
  各 Stage 设计/验收/完成报告、`总结报告.md`、README 均存在且非空。
- **期望**：全部存在。

## 3. 结论（v1.0.1 补完轮次）

| 项 | 结果 |
|---|---|
| S7-1 | ✅ 通过（宿主等效验证 `acceptance_fixtures_test.dart` 全绿） |
| S7-2 | ✅ 用例就绪（`integration_test/player_test.dart`）；设备执行待用户连接 |
| S7-3 | ✅ 用例就绪（同上） |
| S7-4 | ✅ 通过 |
